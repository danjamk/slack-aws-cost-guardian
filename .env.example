# Slack AWS Cost Guardian - Environment Configuration
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control

# =============================================================================
# Deployment Environment
# =============================================================================
# Controls which environment to deploy to (dev, staging, prod)
# Can be overridden on command line: make deploy ENV=prod
ENV=dev
AWS_REGION=us-east-1

# =============================================================================
# Slack Configuration
# =============================================================================
# Create a Slack App at: https://api.slack.com/apps
# Then configure Incoming Webhooks for two channels

SLACK_WEBHOOK_CRITICAL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
SLACK_WEBHOOK_HEARTBEAT=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Signing Secret (required for interactive buttons and @mentions)
# Found at: Slack App -> Basic Information -> App Credentials -> Signing Secret
SLACK_SIGNING_SECRET=your-signing-secret-here

# Bot Token (required for @mentions and DMs)
# Found at: Slack App -> OAuth & Permissions -> Bot User OAuth Token
# Requires: app_mentions:read, chat:write, im:history, im:read, im:write scopes
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# =============================================================================
# LLM API Keys (for AI-powered analysis)
# =============================================================================
# At least one is required for AI analysis. If neither is set, alerts will
# still work but without AI-generated insights.

# Anthropic Claude API (recommended)
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Anthropic Admin API Key (for cost collection)
# Required for Claude API cost tracking. Requires an Organization account.
# Get your key at: https://console.anthropic.com/ -> Settings -> Admin Keys
# This is different from ANTHROPIC_API_KEY - admin keys start with sk-ant-admin-
# ANTHROPIC_ADMIN_API_KEY=sk-ant-admin-your-key-here

# Enable Anthropic cost collection (set to true once ANTHROPIC_ADMIN_API_KEY is configured)
# ANTHROPIC_COSTS_ENABLED=true

# OpenAI API (alternative)
# Get your key at: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-key-here

# =============================================================================
# Optional: GitHub Token (for gh CLI if using multiple accounts)
# =============================================================================
# GH_TOKEN=ghp_your-token-here